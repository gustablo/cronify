<%= form_with model: @cron_job do |form| %>
  <%= form.text_field :name, placeholder: "Name", class: "input" %>
  
  <%= form.text_field :expression, placeholder: "Cron Expression", class: "input" %>

  <%= form.submit class: "btn btn-primary" %>
<% end %>

